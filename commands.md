# Commands Used

## Docker

```bash
docker --version
docker compose version
docker images
docker ps
docker ps -a
docker run hello-world
docker run -it ubuntu bash
docker volume ls
docker network ls
docker logs portairner
```

## Linux

```bash
hostname -I
ip addr show
systemctl is-enabled docker
systemctl is-active docker
curl -k https://localhost:9443
```
